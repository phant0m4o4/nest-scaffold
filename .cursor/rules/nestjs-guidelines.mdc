---
alwaysApply: true
---

## NestJS 专用规范

### 基本原则

- .env 文件已存在.
- 使用模块化架构。
- 将API封装在模块中。
  - 每个主域名/路由一个模块。
  - 一个控制器对应其路由。
    - 以及其他用于次要路由的控制器。
  - 包含数据类型的DTOs文件夹。
    - 使用class-validator验证的输入DTO。
    - 为输出声明简单类型。
  - 包含业务逻辑和持久化的services模块。
    - 使用DrizzleORM的实体进行数据持久化。
    - 每个实体一个服务。
  - 控制器 的设计参考 @src/app/api/demo/demo.controller.ts 文件
  - 控制器相关服务 的设计参考 @src/app/services/demo/demo.service.ts 文件
  - 控制器相关DTO 的设计参考 @src/app/api/demo/dtos/ 文件夹

### 文件命名规范

- 统一使用 kebab-case + dots 的命名格式：`xxx-xxxx.xxx.xx`
  - 基名（文件主体）使用 kebab-case（短横线连接）：如 `user-profile`、`access-key`。
  - 职责/类型后缀使用点号连接：`user-profile.controller.ts`、`user-profile.service.ts`。
- 常见后缀约定：
  - 模块：`*.module.ts`
  - 控制器：`*.controller.ts`
  - 服务：`*.service.ts`
  - 守卫：`*.guard.ts`
  - 拦截器：`*.interceptor.ts`
  - 过滤器：`*.filter.ts`
  - 管道：`*.pipe.ts`
  - 装饰器：`*.decorator.ts`
  - 仓库：`*.repository.ts`
  - 数据结构：`*.dto.ts` / `*.interface.ts` / `*.type.ts`
  - 数据库Schema：`*.schema.ts`
  - Passport 相关：`*.auth.guard.ts`、`*.passport.module.ts`
- 示例：
  - `access-key.module.ts`
  - `access-key.controller.ts`
  - `access-key.service.ts`
  - `jwt-auth.guard.ts`
  - `user.repository.ts`
  - `api-keys.schema.ts`

### 目录命名规范

- 统一使用 kebab-case（短横线分隔小写单词），尽量语义化：
  - `user-profile/`、`access-key/`、`api-keys/`、`jwt/`、`distributed-lock/`
- 常见目录约定：
  - 应用模块根：`src/app/<feature>/`
  - 通用模块根：`src/common/<module>/`
  - DTOs：`dtos/`
  - 仓库：`repositories/`
  - 装饰器：`decorators/`
  - 守卫/拦截器/管道：`guards/`、`interceptors/`、`pipes/`（项目当前放在 common 下的对应模块中）
  - 数据库：`database/schemas/`、`database/utils/`
  - 测试：`__tests__/`（保留例外，双下划线）
- 示例：
  - `src/common/modules/passport/AkSk/`
  - `src/common/modules/passport/jwt/`
  - `src/app/repositories/`
  - `src/database/schemas/`

### 部署与并发

- 项目优先考虑分布式部署；本地开发默认多进程/并发启动（如多实例、热重载并行编译）。
- 编码需注意并发问题：
  - 无状态设计：业务状态放数据库/缓存，避免进程内共享状态。
  - 幂等性：外部可重试接口需提供幂等保障（去重键、唯一约束、乐观锁等）。
  - 分布式锁：涉及跨实例竞争资源（如任务处理、结算、对账）使用 `DistributedLockModule`。
  - 事务与一致性：关键写入使用数据库事务；必要时采用「最终一致性 + 事务外补偿」。
  - 队列解耦与限流：高并发写入/重试场景优先使用队列模块（BullMQ），设置并发、重试与死信策略。
  - 计时与随机：禁止依赖 `setTimeout` 做业务时序，改为队列/调度；避免跨实例漂移导致竞态。
  - 缓存：使用集中式缓存（Redis），谨慎本地 LRU；设置合理 TTL 与 Key 规范。

### 通用模块

- 通用代码目录固定为 `@/common`，存放可跨项目复用的代码。
  - 如需发布为独立包，可从该目录抽取为 npm 包。
  - `@/common` 仅包含无业务耦合的通用能力。
- 创建通用模块用于应用程序间共享、可重用的代码。
  - 此模块应包括：
    - Configs：全局配置设置。
    - Decorators：可重用的自定义装饰器。
    - DTOs：通用数据传输对象。
    - Guards：基于角色或权限的访问控制守卫。
    - Interceptors：用于请求/响应操作的共享拦截器。
    - Notifications：处理应用程序范围通知的模块。
    - Services：跨模块可重用的服务。
    - Types：通用TypeScript类型或接口。
    - Utils：辅助函数和工具。
    - Validators：一致输入验证的自定义验证器。

### 核心模块功能

- 异常处理的全局过滤器。
- 请求管理的全局中间件。
- 权限管理的守卫。
- 请求处理的拦截器。

### 测试

- 所有测试优先使用 @https://docs.nestjs.com/fundamentals/testing 中的风格.
- 集成测试配合使用testcontainers库来生成环境
- 测试文件放在\_\_tests\_\_文件夹中。
  - 单元测试文件名以.spec.ts结尾。
  - 集成测试文件名以.e2e-spec.ts结尾。
- 使用中文写测试文案.
- 使用@package.json中的命令运行测试。
- 使用标准Jest框架进行测试。
- 为每个控制器和服务编写测试。
- 为每个API模块编写端到端测试。

## NestJS 专用规范

### 基本原则

- .env 文件已存在.
- 使用模块化架构。
- 将API封装在模块中。
  - 每个主域名/路由一个模块。
  - 一个控制器对应其路由。
    - 以及其他用于次要路由的控制器。
  - 包含数据类型的DTOs文件夹。
    - 使用class-validator验证的输入DTO。
    - 为输出声明简单类型。
  - 包含业务逻辑和持久化的services模块。
    - 使用DrizzleORM的实体进行数据持久化。
    - 每个实体一个服务。
  - 控制器 的设计参考 @src/app/api/demo/demo.controller.ts 文件
  - 控制器相关服务 的设计参考 @src/app/services/demo/demo.service.ts 文件
  - 控制器相关DTO 的设计参考 @src/app/api/demo/dtos/ 文件夹

### 文件命名规范

- 统一使用 kebab-case + dots 的命名格式：`xxx-xxxx.xxx.xx`
  - 基名（文件主体）使用 kebab-case（短横线连接）：如 `user-profile`、`access-key`。
  - 职责/类型后缀使用点号连接：`user-profile.controller.ts`、`user-profile.service.ts`。
- 常见后缀约定：
  - 模块：`*.module.ts`
  - 控制器：`*.controller.ts`
  - 服务：`*.service.ts`
  - 守卫：`*.guard.ts`
  - 拦截器：`*.interceptor.ts`
  - 过滤器：`*.filter.ts`
  - 管道：`*.pipe.ts`
  - 装饰器：`*.decorator.ts`
  - 仓库：`*.repository.ts`
  - 数据结构：`*.dto.ts` / `*.interface.ts` / `*.type.ts`
  - 数据库Schema：`*.schema.ts`
  - Passport 相关：`*.auth.guard.ts`、`*.passport.module.ts`
- 示例：
  - `access-key.module.ts`
  - `access-key.controller.ts`
  - `access-key.service.ts`
  - `jwt-auth.guard.ts`
  - `user.repository.ts`
  - `api-keys.schema.ts`

### 目录命名规范

- 统一使用 kebab-case（短横线分隔小写单词），尽量语义化：
  - `user-profile/`、`access-key/`、`api-keys/`、`jwt/`、`distributed-lock/`
- 常见目录约定：
  - 应用模块根：`src/app/<feature>/`
  - 通用模块根：`src/common/<module>/`
  - DTOs：`dtos/`
  - 仓库：`repositories/`
  - 装饰器：`decorators/`
  - 守卫/拦截器/管道：`guards/`、`interceptors/`、`pipes/`（项目当前放在 common 下的对应模块中）
  - 数据库：`database/schemas/`、`database/utils/`
  - 测试：`__tests__/`（保留例外，双下划线）
- 示例：
  - `src/common/modules/passport/AkSk/`
  - `src/common/modules/passport/jwt/`
  - `src/app/repositories/`
  - `src/database/schemas/`

### 部署与并发

- 项目优先考虑分布式部署；本地开发默认多进程/并发启动（如多实例、热重载并行编译）。
- 编码需注意并发问题：
  - 无状态设计：业务状态放数据库/缓存，避免进程内共享状态。
  - 幂等性：外部可重试接口需提供幂等保障（去重键、唯一约束、乐观锁等）。
  - 分布式锁：涉及跨实例竞争资源（如任务处理、结算、对账）使用 `DistributedLockModule`。
  - 事务与一致性：关键写入使用数据库事务；必要时采用「最终一致性 + 事务外补偿」。
  - 队列解耦与限流：高并发写入/重试场景优先使用队列模块（BullMQ），设置并发、重试与死信策略。
  - 计时与随机：禁止依赖 `setTimeout` 做业务时序，改为队列/调度；避免跨实例漂移导致竞态。
  - 缓存：使用集中式缓存（Redis），谨慎本地 LRU；设置合理 TTL 与 Key 规范。

### 通用模块

- 通用代码目录固定为 `@/common`，存放可跨项目复用的代码。
  - 如需发布为独立包，可从该目录抽取为 npm 包。
  - `@/common` 仅包含无业务耦合的通用能力。
- 创建通用模块用于应用程序间共享、可重用的代码。
  - 此模块应包括：
    - Configs：全局配置设置。
    - Decorators：可重用的自定义装饰器。
    - DTOs：通用数据传输对象。
    - Guards：基于角色或权限的访问控制守卫。
    - Interceptors：用于请求/响应操作的共享拦截器。
    - Notifications：处理应用程序范围通知的模块。
    - Services：跨模块可重用的服务。
    - Types：通用TypeScript类型或接口。
    - Utils：辅助函数和工具。
    - Validators：一致输入验证的自定义验证器。

### 核心模块功能

- 异常处理的全局过滤器。
- 请求管理的全局中间件。
- 权限管理的守卫。
- 请求处理的拦截器。
