---
alwaysApply: true
---

## RESTful API 规范

- 使用RESTful API 规范。
- 使用HTTP动词来表示操作。
  - GET：获取资源。
  - POST：创建资源。
  - PUT：更新资源。
  - DELETE：删除资源。
  - PATCH：部分更新资源。
- 使用HTTP状态码来表示操作结果。
- 返回数据格式为：

```json
// 创建成功：
{
  "data": {}
  "meta": {} //可选
}
// 或者无返回

// 更新成功：
{
  "data": {}
  "meta": {} //可选
}
// 或者无返回

// 删除成功：
// 无返回

// 获取成功：
{
  "data": {}
  "meta": {} //可选
}

// 普通失败：
{
  "error": "Bad Request",
  "message":"message",
}
// 校验失败：
{
  "statusCode": 422,
  "message": "Validation failed",
  "errors": {
    "email": { "rule": "isEmail", "message": "邮箱格式不正确" },
    "password": { "rule": "minLength", "message": "至少 6 位字符" }
    }
}
```

### 分页规范

- 请求（游标分页）：

```json
{
  "cursor": 10,
  "limit": 30,
  "order": "desc"
}
```

- 响应（统一响应包裹）：

```json
{
  "statusCode": 200,
  "data": [
    { "id": 1, "name": "item-1" },
    { "id": 2, "name": "item-2" }
  ],
  "meta": {
    "nextCursor": 12
  }
}
```

- 字段说明：
  - `cursor`：上一页返回的 `meta.nextCursor`；缺省代表第一页。
  - `limit`：每页条数，默认 30，最大 100（示例参考 `FindManyDemoDto`）。
  - `order`：排序方向，`asc` 或 `desc`。
  - `data`：数据数组，元素为对应资源的响应 DTO。
  - `meta.nextCursor`：下一页游标，无下一页时为 `null`（参见 `CursoredPaginationResponseDto`）。

### CRUD 控制器方法命名规范

#### 标准 CRUD 操作

- **创建资源**：

  - 控制器方法：`create`
  - 服务方法：`create`
  - 请求 DTO：`Create{ResourceName}RequestDto`
  - 响应 DTO：`{ResourceName}ResponseDto`

- **获取单个资源**：

  - 控制器方法：`findOne`
  - 服务方法：`findOne`
  - 参数 DTO：`FindOne{ResourceName}ParamDto`
  - 响应 DTO：`{ResourceName}ResponseDto`

- **获取多个资源**：

  - 控制器方法：`findMany`
  - 服务方法：`findMany`
  - 请求 DTO：`FindMany{ResourceName}RequestDto`
  - 响应 DTO：`{ResourceName}ResponseDto[]`

- **更新资源**：

  - 控制器方法：`update`
  - 服务方法：`update`
  - 参数 DTO：`Update{ResourceName}ParamDto`
  - 请求 DTO：`Update{ResourceName}RequestDto`
  - 响应 DTO：`{ResourceName}ResponseDto`

- **删除资源**：
  - 控制器方法：`remove`
  - 服务方法：`remove`
  - 参数 DTO：`Remove{ResourceName}ParamDto`
  - 响应：无数据返回

#### 特殊操作命名规范

- **查找特定资源**：

  - 控制器方法：`findOne{Action}`（如 `findOneText`、`findOneByEmail`）
  - 服务方法：`findOne{Action}`
  - 请求 DTO：`FindOne{Action}RequestDto`
  - 响应 DTO：`{Action}ResponseDto`

- **验证操作**：

  - 控制器方法：`verify`
  - 服务方法：`verify`
  - 请求 DTO：`Verify{ResourceName}RequestDto`
  - 响应 DTO：`Verify{ResourceName}ResponseDto`

- **统计操作**：
  - 控制器方法：`count`
  - 服务方法：`count`
  - 请求 DTO：`Count{ResourceName}RequestDto`
  - 响应 DTO：`Count{ResourceName}ResponseDto`

### DTO 命名规范

#### 请求 DTO 命名

- **创建请求**：`Create{ResourceName}RequestDto`

  - 示例：`CreateUserRequestDto`、`CreateTaskRequestDto`

- **更新请求**：`Update{ResourceName}RequestDto`

  - 示例：`UpdateUserRequestDto`、`UpdateTaskRequestDto`

- **查询请求**：`FindMany{ResourceName}RequestDto`

  - 示例：`FindManyUserRequestDto`、`FindManyTaskRequestDto`

- **特殊查询请求**：`FindOne{Action}RequestDto`

  - 示例：`FindOneTextRequestDto`、`FindOneByEmailRequestDto`

- **参数 DTO**：`{Action}{ResourceName}ParamDto`
  - 示例：`FindOneUserParamDto`、`UpdateTaskParamDto`

#### 响应 DTO 命名

- **资源响应**：`{ResourceName}ResponseDto`

  - 示例：`UserResponseDto`、`TaskResponseDto`

- **特殊响应**：`{Action}ResponseDto`

  - 示例：`VerifyResponseDto`、`CountResponseDto`

- **分页响应**：继承通用分页响应
  - 使用 `CursoredPaginationResponseDto<{ResourceName}ResponseDto>`

#### 接口命名规范

- **业务接口**：`I{ResourceName}{Action}`

  - 示例：`IUserCreate`、`ITaskPayload`

- **服务接口**：`I{ResourceName}Service`
  - 示例：`IUserService`、`ITaskService`

#### 文件组织规范

```
src/app/{module}/
├── {resource}.controller.ts
├── {resource}.service.ts
├── {resource}.module.ts
├── dtos/
│   ├── create-{resource}-request.dto.ts
│   ├── update-{resource}-request.dto.ts
│   ├── find-many-{resource}-request.dto.ts
│   ├── find-one-{resource}-param.dto.ts
│   ├── {resource}-response.dto.ts
│   └── verify-{resource}-response.dto.ts
├── interfaces/
│   ├── {resource}-payload.interface.ts
│   └── {resource}-proof.interface.ts
└── __tests__/
    └── {resource}.e2e-spec.ts
```

#### 示例

```typescript
// 控制器示例
@Controller('/users')
export class UserController {
  @Post()
  async create(@Body() dto: CreateUserRequestDto) {
    return this.userService.create(dto);
  }

  @Get(':id')
  async findOne(@Param() param: FindOneUserParamDto) {
    return this.userService.findOne(param.id);
  }

  @Get()
  async findMany(@Query() query: FindManyUserRequestDto) {
    return this.userService.findMany(query);
  }

  @Put(':id')
  async update(
    @Param() param: UpdateUserParamDto,
    @Body() dto: UpdateUserRequestDto,
  ) {
    return this.userService.update(param.id, dto);
  }

  @Delete(':id')
  async remove(@Param() param: RemoveUserParamDto) {
    return this.userService.remove(param.id);
  }
}
```
