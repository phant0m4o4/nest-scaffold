# 验收报告：务实顺序依赖升级（Node 24 / Redis 8 / ioredis 6 / pnpm 11 / ESLint 10）

分支：`chore/deps-pragmatic-upgrade`  
对照：[task-acceptance.md](../../../.claude/skills/nest-scaffold/reference/task-acceptance.md)、[engineering-conventions.md](../../../.claude/skills/nest-scaffold/reference/engineering-conventions.md)

---

## 第 0 档 · 循环 review 记录

| 轮次 | 视角 | 发现 | 处置 |
|------|------|------|------|
| 1 | Docker 安装层 / pnpm 11 配置一致性 | **高**：Dockerfile `build`/`prod-deps` 未 COPY `pnpm-workspace.yaml`，`pnpm install` 在缺 overrides/allowBuilds/exclude 时会按默认 `minimumReleaseAge=1440` 失败，或随后 `LOCKFILE_CONFIG_MISMATCH` / `IGNORED_BUILDS` | 已在两处 `COPY` 增加 `pnpm-workspace.yaml`；`docker build --target build` exit 0 |
| 1 | Redis / ioredis 6 | 无：默认 RESP3 + `replyMapping: legacy` 与 v5 回复形状兼容；工厂无需显式 `protocol` | — |
| 1 | 工程不变量 R1–R4 / C2 / S1–S3 / T1–T3 | 无新增违反 | — |
| 2（换视角复审，含修复面） | Docker / pnpm 三方一致 / Redis 冒烟 | **本轮零发现**（连续空轮达成） | — |
| 3 | 第二轮大版本：argon2 / testcontainers / bull-board / inquirer | bullmq 6 探路：双 peer 不满足，已回退 5；其余全绿无代码改动 | 保持 bullmq 5 |
| 4（换视角） | peer 矩阵 + e2e（testcontainers 12 默认 healthcheck wait） | **本轮零发现** | — |

---

## 第 1 档 · 自动化测试清单

### 测前覆盖清单

| 必测类别 | 是否适用 | 覆盖方式 |
|----------|----------|----------|
| 正常路径 | 是 | 既有单测 + e2e 全绿 |
| 边界 / 失败 | 是（Redis 关闭、类型断言） | 既有 redis factory / connection 单测 |
| 幂等 / 并发 | 无（本次无业务写路径变更） | — |
| 基础设施可构建 | 是 | `docker build --target build` |
| lint / 类型 | 是 | `pnpm lint:check` + `pnpm build`（TSC） |

### 实际用例对照

| 行为 | 断言 / 结果 | 对照 |
|------|-------------|------|
| 单元测试套件 | 23 files / 149 tests passed | 覆盖 |
| Redis factory e2e（`redis:8-alpine`） | e2e 套件内通过 | 覆盖 |
| Demo cursor e2e | e2e 通过 | 覆盖 |
| ESLint 10 flat config | `lint:check` 0 error | 覆盖 |
| 生产镜像构建层 | docker build exit 0 | 覆盖 |

缺口：无（未新增业务逻辑，回归既有套件 + 镜像构建）。

取证：[`evidence/gates.txt`](./evidence/gates.txt)

---

## 第 2 档 · 人工验收记录

后端依赖升级，无页面/视觉残留。人工档：**无**（自动化已驱动到门禁结论）。

请着重看（若抽查）：

1. `evidence/gates.txt` 末尾：单测 149、e2e 12 全绿  
2. Docker 构建日志：`Done in … using pnpm v11.20.0` 且 TSC 0 issues  
3. `pnpm-workspace.yaml` 的 `allowBuilds` / `minimumReleaseAgeExclude` 注释是否可接受  

---

## 第 3 档 · 审核导读

### 重点审

- `Dockerfile`：安装层必须带上 `pnpm-workspace.yaml`（已修；缺了会假绿本地、CI `docker` 红）
- `pnpm-workspace.yaml`：`allowBuilds`、`overrides.ioredis`、临时 `minimumReleaseAgeExclude`
- `src/app/repositories/common/pgsql/base.repository.ts`：`_tableForQuery`（drizzle 0.45.2 泛型 `from()` 兼容）
- Redis 镜像与 e2e 钉死 `redis:8-alpine`

### 看一眼

- `package.json` / `pnpm-lock.yaml` / Nest·Drizzle·vitest 等同主版本 bump  
- ESLint/prettier 自动修的无关断言与换行  
- README / CI `node-version: 24`  

---

## 交付物清单

| 文件 | 作用 |
|------|------|
| `package.json` | Node `>=24`、pnpm `>=11`、`packageManager=pnpm@11.20.0`；依赖升级；移除 `#pnpm` 字段 |
| `pnpm-workspace.yaml` | **新增**：overrides / allowBuilds / minimumReleaseAgeExclude |
| `pnpm-lock.yaml` | 锁文件重生（pnpm 11 store） |
| `Dockerfile` | Node 24 镜像 + COPY workspace yaml |
| `docker-compose.yml` | Redis 8 |
| `.github/workflows/ci.yml` | Node 24 |
| `README.md` | 环境要求同步 |
| `src/.../pgsql/base.repository.ts` | drizzle 类型适配 |
| 若干源文件 | typescript-eslint/prettier 自动修 |
| Redis e2e / testing.md | 镜像 `redis:8-alpine` |
| package.json（第二轮） | argon2 0.45、inquirer 14、testcontainers 12、@bull-board 8、@types/supertest 7；bullmq 钉 5.81 |
| `reports/deps/pragmatic-upgrade/` | 本验收报告 |

## 规格偏差回写

- **做了规格外**：pgsql `_tableForQuery`（为消化 drizzle 补丁类型收紧，属升级连带修复）  
- **第二轮已做（原「值得排期 / 暂缓可试」）**：argon2 0.45、testcontainers 12、inquirer 14、@types/supertest 7、@bull-board 8（与 bullmq 5 兼容）  
- **探路后仍不做**：bullmq 6——`@nestjs/bullmq@11` peer 只要 `^3||^4||^5`，且 `@bull-board/api@8` peer 要 `bullmq@^5.79`；强升会双 peer 红  
- **仍不做**：NestJS 12（仅 alpha）、TypeScript 7、`@types/node` 26（运行时钉 Node 24）  
- **已知非阻塞**：`@swc/cli` 要 chokidar^5，与 Nest/`@angular-devkit` 的 chokidar^4 互斥，维持警告  

## 改动面自查

- 纯本任务依赖/工具链升级，无业务功能越界  
- 无密钥、真实端点、本机绝对路径、个人信息、AI 署名入库  
- 报告 evidence 仅含命令输出  

---

## 门禁结果（摘要）

- `node` v24.19.0 / `pnpm` 11.20.0  
- `pnpm lint:check` ✅  
- `pnpm build` ✅  
- `pnpm test` ✅ 149  
- `pnpm test:e2e` ✅ 12（含 testcontainers 12 + Redis 8）  
- `docker build --target build` ✅  

### 第二轮版本钉死

| 包 | 版本 |
|----|------|
| argon2 | 0.45.1 |
| inquirer | 14.0.2 |
| testcontainers | 12.0.4 |
| @bull-board/* | 8.5.0 |
| @types/supertest | 7.2.1 |
| bullmq | 5.81.3（保持主版本 5） |
