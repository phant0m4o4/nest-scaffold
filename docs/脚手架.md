# 安装 nest

命令行

```shell
APP_NAME=nest-scaffold
pnpm i -g @nestjs/cli
nest new ${APP_NAME}
cd ./${APP_NAME}
touch ./.env
rm ./src/app.controller.spec.ts ./src/app.controller.ts ./src/app.service.ts ./src/app.module.ts
nest g mo app
```

### swc

```shell
pnpm i --save-dev @swc/cli @swc/core
```

# 修改 package.json

```json
{
  "scripts": {
    "start:swc": "nest start --builder swc --type-check",
    "start:swc:dev": "nest start --builder swc --type-check --watch",
    "start:swc:debug": "nest start --builder swc --type-check --debug --watch"
  }
}
```

### viest

```shell
pnpm i --save-dev vitest unplugin-swc @swc/core @vitest/coverage-v8 testcontainers
```

# 修改 package.json

```json
{
  "scripts": {
    "vitest": "vitest run",
    "vitest:watch": "vitest",
    "vitest:cov": "vitest run --coverage",
    "vitest:debug": "vitest run --inspect-brk --no-file-parallelism --test-timeout=0",
    "vitest:e2e": "vitest run --config ./vitest-e2e.config.ts"
  }
}
```

# 添加vitest.config.ts

```ts
import { resolve } from 'path';
import swc from 'unplugin-swc';
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['**/*.spec.ts'],
    globals: true,
    root: './',
  },
  plugins: [
    // This is required to build the test files with SWC
    swc.vite({
      // Explicitly set the module type to avoid inheriting this value from a `.swcrc` config file
      module: { type: 'es6' },
    }),
  ],
  resolve: {
    alias: {
      // Ensure Vitest correctly resolves TypeScript path aliases
      '@': resolve(__dirname, './src'),
    },
  },
});
```

# 创建 vitest-e2e.config.ts

```ts
import { defineConfig } from 'vitest/config';
import config from './vitest.config';

export default defineConfig({
  ...config,
  test: {
    ...config.test,
    include: ['**/*.e2e-spec.ts'],
  },
});
```

### 自动格式化 .vscode/settings.json 复制 rules

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "eslint.validate": ["javascript", "typescript"],
  "prettier.singleQuote": true,
  "prettier.trailingComma": "all"
}
```

### commitizen

https://github.com/commitizen/cz-cli

命令行

```shell
pnpm install -g commitizen
pnpm install -D commitizen cz-conventional-changelog
```

修改 package.json 文件

```json
{
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
```

### 修改 package.json

```shell
pnpm install cross-env
```

```json
"start:dev": "cross-env NODE_ENV=\"development\" nest start --watch",
```

### tsconfig.json 中添加 path alias

```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

### 单元测试

# 删除 package.json 中的 jest相关部分

```shell
touch jest.config.ts
```

```ts
export const config = {
  moduleFileExtensions: ['js', 'json', 'ts'],
  rootDir: 'src',
  testEnvironment: 'node',
  testRegex: '.spec.ts$',
  transform: {
    '^.+\\.(t|j)s$': 'ts-jest',
  },
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/$1',
  },
};
export default {
  ...config,
  collectCoverageFrom: ['**/*.(t|j)s'],
  coverageDirectory: '../coverage',
};
```

### 集成测试

删除 ./test 目录

```shell
rm -rf ./test
touch jest-e2e.config.ts
```

```ts
import { config } from './jest.config';
export default {
  ...config,
  testRegex: '.e2e-spec.ts$',
};
```

package.json

````json
 {
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:e2e": "jest --config ./jest-e2e.config.ts",
  },
 }


### 添加 .env

```ini
APP_NAME=nest-scaffold
PORT=3000

#MySQL
MYSQL_HOST=Localhost
MYSQL_PORT=3306
MYSQL_DATABASE=${APP_NAME}
MYSQL_USER=root
MYSQL_PASSWORD=root_password

#Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSPWORD=redis_password
````

### Docker 环境

创建 \docker-compose.yml 文件

```yml
#mysql，redis，phpmyadmin 服务 用于 nestjs 项目
services:
  mysql:
    container_name: ${APP_NAME}-mysql
    image: mysql:8.0
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
    #    image: mysql:5.7
    #    platform: linux/amd64
    #    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'root_password'
      MYSQL_DATABASE: ${MYSQL_DATABASE:-dev_db}
    ports:
      - ${MYSQL_PORT:-3306}:3306
    networks:
      - nestjs

  phpmyadmin:
    container_name: ${APP_NAME}-phpmyadmin
    image: phpmyadmin
    restart: always
    ports:
      - '8080:80'
    environment:
      PMA_HOST: mysql # 这里的 db-dev 是 db-dev 服务的名称
      PMA_PORT: ${MYSQL_PORT:-3306}
      PMA_USER: 'root'
      PMA_PASSWORD: 'root_password'
      UPLOAD_LIMIT: 100000000
    depends_on:
      - mysql
    networks:
      - nestjs

  redis:
    container_name: ${APP_NAME}-redis
    image: redis
    command: redis-server --requirepass ${REDIS_PASSWORD:-redis_password}
    restart: always
    ports:
      - ${REDIS_PORT:-6379}:6379
    networks:
      - nestjs

  phpRedisAdmin:
    container_name: ${APP_NAME}-phpRedisAdmin
    image: erikdubbelboer/phpredisadmin
    restart: always
    ports:
      - '8081:80'
    environment:
      REDIS_1_HOST: redis
      REDIS_1_PORT: ${REDIS_PORT:-6379}
      REDIS_1_AUTH: ${REDIS_PASSWORD:-redis_password}
    depends_on:
      - redis
    networks:
      - nestjs

networks:
  nestjs:
    driver: bridge
```

### 启动开发环境

命令行

```shell
# 创建并启动容器 会读取.env 变量创建
docker-compose -p ${APP_NAME} up -d
# 停止并删除容器
docker-compose down
# 启动容器
docker-compose start
# 停止容器
docker-compose stop
```

### 安装 config 模块

https://docs.nestjs.com/technigues/configuration

命令行

```shell
pnpm install @nestjs/config class-validator class-transformer
pnpm install -D cross-env
mkdir ./src/common
mkdir ./src/common/utils
touch ./src/common
touch ./src/common/utils/register-env-as.ts
mkdir ./src/configs
touch ./src/configs/app.config.ts
```

### dayjs

命令行

```shell
pnpm install dayjs
```

修改 tsconfig.json 文件

```shell
{
    "compilerOptions": {
        "esModuleInterop": true,
    }
}
```

# Swagger

命令行

```shell
pnpm install @nestjs/swagger
```

修改 nest-cli.json 文件

```shell
{
    "compilerOptions": {
        "plugins": [{
            "name": "@nestjs/swagger",
            "options": {
                "classValidatorShim": true,
                "introspectComments": true
            }
        }]
    }
}
```

# Drizzle ORM

### 安装 drizzle orm

命令行

```shell
pnpm install drizzle-orm drizzle-kit mysql2 drizzle-seed inquirer @faker-js/faker
touch ./drizzle.config.ts
touch ./src/configs/database.config.ts
mkdir -p ./src/database/schemas
nest g mo common/modules/database
nest g s common/modules/database
pnpm install dayjs
```

修改 drizzle.config.ts

```typescript
import 'dotenv/config';
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  out: './drizzle',
  schema: './src/database/schema.ts',
  dialect: 'mysql',
  dbCredentials: {
    host: process.env.MYSQL_HOST!,
    port: parseInt(process.env.MYSQL_PORT!),
    database: process.env.MYSQL_DATABASE!.replace(
      '${APP_NAME}', // 因为数据库名可能是根据 APP_NAME 动态生成的
      process.env.APP_NAME!,
    ),
    user: process.env.MYSQL_USER,
    password: process.env.MYSQL_PASSWORD,
  },
});
```

.env 文件，添加：

```shell
#MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=${APP_NAME}
MYSQL_USER=root
MYSQL_PASSWORD=root_password

#Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
```

创建并编写文件 drizzle.config.ts

创建并编写文件 src/configs/database.config.ts

修改 src/common/modules/database/database.module.ts

修改 src/common/modules/database/database.service.ts

### 坑

定义表的时候一定要加primaryKey() 不然 insert 拿不到 id

# JWT

### 安装 jwt

命令行

```shell
pnpm install --save jsonwebtoken
```

修改 .env 文件，添加:

```shell
#Auth
JWT_SECRET=secret
JWT_EXPIRES_IN=1h
```

### 安装 argon2

```shell
pnpm install argon2
```

### i18n

```shell
pnpm install nestjs-i18n
```

修改 nest-cli.json

```json
{
  "compilerOptions": {
    "assets": [{ "include": "app/i18n/**/*", "watchAssets": true }]
  }
}
```

# CACHE

```shell
pnpm install ioredis
```

#分布式锁
tsconfig.json

<!-- ```json
{
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node"
  }
}
``` -->

```shell
pnpm install redlock
```

# 限流

```shell
pnpm install @nestjs/throttler
```

# 队列

```shell
pnpm install @nestjs/bullmq bullmq
pnpm install -D @bull-board/nestjs @bull-board/api @bull-board/express
```

# session

```shell
pnpm install express-session axios
pnpm install -D @types/express-session
```

# 日志

```shell
pnpm install nestjs-pino pino-pretty pino-roll
```
